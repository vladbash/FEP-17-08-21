function avgOfNumbersArray(numbers) {
    return numbers.reduce((acc, e) => acc + e, 0) / numbers.length;
}

// TODO: implement this function!!!
function medianOfNumbersArray(numbers) {
    return 0;
}