// const utils = require('./utils');
const { getRandomNumber, printCurrentDate } = require('./utils');
const logger = require('./logger');

// const logChannel = `log_server_${new Date().toISOString()}.log`;
// logger.appendDataToLog('server app started', logChannel)
// console.log(getRandomNumber());
// logger.appendDataToLog(`getRandomNumber() was applied with the result ${getRandomNumber()}`, logChannel);
// printCurrentDate();
logger.demoFunc();